# Este módulo debe proporcionar funciones que retornen:
# - Para microscópico: arrays de posiciones x_i(t) y velocidades v_i(t)
# Formato esperado: numpy arrays con shape (n_time_steps, n_spatial_points)